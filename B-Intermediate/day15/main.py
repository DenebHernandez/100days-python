'''Coffee machine replica on python'''

from functions import insert_coins, user_input

# Promt user by asking which coffee would they like to order
order = user_input("order")

# Turn off the machine if the user inputs "off"

# Print report of current resources if user enters "report"

# Check if resources are sufficient after getting an order and
# before serving it

# Process coins, prompt for an amount of quarters, dimes, nickels
# and pennies
payment = insert_coins()

# Check if the money inserted is enough to pay for the order

# Make coffee by adding the money to a total bank and reducing
# the stored amount of resources
